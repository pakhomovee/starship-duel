# Starship Duel — runtime dependencies
# Core simulator (starship_duel/game, env, bots) needs NONE of these; it is
# pure stdlib.  These enable the web UI and the RL adapters.

# --- web interface (starship_duel/web) ---
fastapi>=0.139
uvicorn[standard]>=0.50
networkx>=3.0     # crossing-free (planar) map layouts; degrades to a circle if absent

# --- RL adapters (starship_duel/rl) ---
numpy>=2.0
gymnasium>=1.0
pettingzoo>=1.25
torch>=2.6

# --- training telemetry (optional; PPO logs scalars if importable) ---
tensorboard>=2.16

# --- tournament scoring (starship_duel/tournament) ---
choix>=0.3.5      # Bradley-Terry pairwise ranking (pulls scipy)

# --- dev ---
pytest>=8.0
httpx>=0.27      # fastapi TestClient (web API tests)
